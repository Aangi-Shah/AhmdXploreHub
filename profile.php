<?php
        // Assume you have obtained the user's name during login
        $userName = "John"; // Replace with the actual user's name
        $initial = strtoupper(substr($userName, 0, 1)); // Get the first letter and convert to uppercase
    ?>